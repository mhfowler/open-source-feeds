  version: '3'
  services:
    osf_dashboard:
      image: '${BUILD_ENV}_osf_dashboard'
      build:
        context: .
        args:
          - BUILD_ENV
      ports:
        - '80:80'
        - '443:443'

